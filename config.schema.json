{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "pluginAlias": "OctopusEnergyLive",
  "pluginType": "platform",
  "singular": false,
  "strictValidation": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Platform Name",
        "default": "Octopus Energy Live"
      },
      "apiKey": {
        "type": "string",
        "format": "password",
        "title": "Octopus API Key",
        "description": "Create an API key in your Octopus Energy account. The Homebridge UI stores this value in its plugin configuration."
      },
      "accountNumber": {
        "type": "string",
        "title": "Octopus Account Number (recommended for live data)",
        "description": "Your account number in A-12345678 format. Used to discover the Home Mini electricity meter device ID automatically."
      },
      "pollSeconds": {
        "type": "number",
        "title": "Polling Interval (seconds)",
        "description": "Defaults to 30 seconds with a Home Mini device ID, otherwise 300 seconds. Octopus rate-limits live telemetry, so values below 30 seconds are not allowed.",
        "minimum": 30
      },
      "homeMiniDeviceId": {
        "type": "string",
        "title": "Home Mini Electricity Meter Device ID (optional)",
        "description": "Optional override for the electricity meter EUI64 device ID, for example 12-34-5A-FF-FF-6B-C7-89. Normally discovered from the account number."
      },
      "import": {
        "type": "object",
        "title": "Electricity Meter",
        "properties": {
          "name": {
            "type": "string",
            "title": "Accessory Name",
            "default": "Electricity Meter"
          },
          "mpan": {
            "type": "string",
            "title": "MPAN"
          },
          "meterSerial": {
            "type": "string",
            "title": "Meter Serial"
          }
        },
        "required": ["mpan", "meterSerial"],
        "additionalProperties": false
      },
      "gas": {
        "type": "object",
        "title": "Gas Meter (Optional)",
        "description": "Octopus gas data is read in half-hour intervals and converted to kWh. Matter and Apple Home do not currently define a native gas meter device. Gas uses an MPRN, not an MPAN.",
        "properties": {
          "name": {
            "type": "string",
            "title": "Accessory Name",
            "default": "Gas Meter"
          },
          "mprn": {
            "type": "string",
            "title": "MPRN",
            "description": "Meter Point Reference Number for the gas supply."
          },
          "meterSerial": {
            "type": "string",
            "title": "Meter Serial"
          },
          "unit": {
            "type": "string",
            "title": "Consumption Unit",
            "description": "This is the native API unit used for conversion. Automatic uses the Octopus account data. SMETS1 is normally kWh; SMETS2 is normally cubic metres. The displayed result is always kWh.",
            "default": "auto",
            "oneOf": [
              { "title": "Automatic (recommended)", "enum": ["auto"] },
              { "title": "Cubic metres (m³ / SMETS2)", "enum": ["m3"] },
              { "title": "Kilowatt-hours (kWh / SMETS1)", "enum": ["kWh"] }
            ]
          },
          "exposeToMatter": {
            "type": "boolean",
            "title": "Experimental Matter Outlet",
            "description": "Expose gas as an electrical energy-monitoring outlet. Apple Home will show a power socket, may not display its gas kWh, and will not treat it as a native gas meter. Leave disabled unless you accept this limitation.",
            "default": false
          },
          "exposeDailyUsageToMatter": {
            "type": "boolean",
            "title": "Show Today's Usage in Matter (Beta)",
            "description": "Adds the standards-based Matter Periodic Energy value for gas used today. Experimental: Apple Home may choose not to display this value. Requires Experimental Matter Outlet and creates a new gas Matter endpoint when enabled.",
            "default": false
          },
          "exposeDailyUsageAccessory": {
            "type": "boolean",
            "title": "Separate Gas Used Today Accessory (Beta)",
            "description": "Creates a separate Matter outlet named Gas Used Today. The true UK-day total is published as kWh periodic energy, while an experimental display proxy maps the same number to kW so Apple Home can show it on the outlet screen (for example, 1.62 kWh appears as 1.62 kW). This is not instantaneous gas power and may affect Apple's electrical energy summaries.",
            "default": false
          },
          "pollMinutes": {
            "type": "number",
            "title": "Gas Refresh Interval (minutes)",
            "description": "How often to check the half-hourly REST source. Defaults to 5 minutes. Experimental Home Mini gas telemetry instead refreshes today's total every 30 minutes.",
            "default": 5,
            "minimum": 5,
            "maximum": 30
          },
          "useLiveTelemetry": {
            "type": "boolean",
            "title": "Experimental Home Mini Gas Telemetry",
            "description": "Use the Home Mini GSME telemetry feed to calculate today's gas usage. Falls back to the official half-hourly REST endpoint automatically. When electricity telemetry is also active, its polling interval is limited to 60 seconds to remain within Octopus API rate limits.",
            "default": false
          },
          "homeMiniDeviceId": {
            "type": "string",
            "title": "Home Mini Gas Meter Device ID (optional)",
            "description": "Optional GSME EUI64 override, for example 12-34-5A-FF-FF-6B-C7-89. Normally discovered automatically from the account number, MPRN, and meter serial."
          }
        },
        "additionalProperties": false
      },
      "export": {
        "type": "object",
        "title": "Export Meter (optional)",
        "properties": {
          "name": {
            "type": "string",
            "title": "Accessory Name",
            "default": "Octopus Export"
          },
          "mpan": {
            "type": "string",
            "title": "MPAN"
          },
          "meterSerial": {
            "type": "string",
            "title": "Meter Serial"
          }
        },
        "additionalProperties": false
      }
    },
    "required": ["apiKey", "import"],
    "additionalProperties": false
  }
}
